/* global it, describe */
import {UtBulkRoutes} from '../index'

describe('A suite for <IndexRoute />', function () {
  it('should contain elements', function () {
    // increase coverage
    UtBulkRoutes({})
  })
})
