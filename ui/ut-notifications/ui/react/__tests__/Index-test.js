/* global it, describe */
import {UtNotificationsRoutes} from '../index'

describe('A suite for <IndexRoute />', function () {
  it('should contain elements', function () {
    // increase coverage
    UtNotificationsRoutes({})
  })
})
